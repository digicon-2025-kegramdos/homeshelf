<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_comiclist_item_root"
    style="@style/Widget.HomeShelf.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.HomeShelf.AppWidgetContainer">

    <ImageView
        android:id="@+id/widget_comiclist_item_icon"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:layout_weight="0"
        android:contentDescription="@string/comic_icon_description"
        android:scaleType="center"
        app:srcCompat="@drawable/ic_launcher_foreground" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_comiclist_item_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/widget_comiclist_item_subtext"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>

</LinearLayout>